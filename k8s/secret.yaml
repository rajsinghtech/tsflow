apiVersion: v1
kind: Secret
metadata:
  name: tsflow
type: Opaque
stringData:
  TAILSCALE_API_KEY: ${TAILSCALE_API_KEY}
  TAILSCALE_TAILNET: ${TAILSCALE_TAILNET}
  TAILSCALE_API_URL: ${TAILSCALE_API_URL}
  TAILSCALE_OAUTH_CLIENT_ID: ${TAILSCALE_OAUTH_CLIENT_ID}
  TAILSCALE_OAUTH_CLIENT_SECRET: ${TAILSCALE_OAUTH_CLIENT_SECRET}
  TAILSCALE_OAUTH_SCOPES: ${TAILSCALE_OAUTH_SCOPES}
  TSFLOW_FLOW_BACKEND: ${TSFLOW_FLOW_BACKEND}
  TSFLOW_S3_BUCKET: ${TSFLOW_S3_BUCKET}
  TSFLOW_S3_PREFIX: ${TSFLOW_S3_PREFIX}
  TSFLOW_S3_ENDPOINT: ${TSFLOW_S3_ENDPOINT}
  TSFLOW_S3_REGION: ${TSFLOW_S3_REGION}
  TSFLOW_S3_ACCESS_KEY_ID: ${TSFLOW_S3_ACCESS_KEY_ID}
  TSFLOW_S3_SECRET_ACCESS_KEY: ${TSFLOW_S3_SECRET_ACCESS_KEY}
  TSFLOW_S3_LOOKBACK: ${TSFLOW_S3_LOOKBACK}
  TSFLOW_S3_MAX_OBJECTS_PER_POLL: ${TSFLOW_S3_MAX_OBJECTS_PER_POLL}
